# API (apps/api/.env)
PORT=3001
DATABASE_URL=postgresql://qualti:qualti@localhost:5433/qualti_dev

# Web (apps/web/.env)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=https://qualti.io

# Optional public form endpoint for the landing-page lead form.
# Use a destination such as Formspree, a future NestJS public lead route, or another HTTPS form endpoint.
# Do not put API keys or secrets in this value.
NEXT_PUBLIC_LEAD_ENDPOINT=
